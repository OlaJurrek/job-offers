import { Kanit, Yantramanav } from "next/font/google";

export const yantramanav = Yantramanav({
  subsets: ["latin"],
  weight: ["400", "500"],
});

export const kanit = Kanit({ subsets: ["latin"], weight: "700" });
